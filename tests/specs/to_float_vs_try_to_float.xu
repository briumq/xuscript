// 测试to_float()和try_to_float()方法的区别

// 测试函数
func test_to_float_vs_try_to_float() {
    println("=== 测试to_float()和try_to_float()方法的区别 ===")

    // 测试to_float()方法（无效输入应该抛出错误）
    println("\n测试to_float()方法（无效输入）")
    println("期望: 抛出错误")
    // 注意：这里的代码会抛出错误，所以我们不会运行它
    // let invalid_float = "invalid".to_float()
    // println("Output: {invalid_float}")
    println("（跳过运行，因为会抛出错误）")

    // 测试try_to_float()方法（无效输入应该返回None）
    println("\n测试try_to_float()方法（无效输入）")
    println("期望: 返回None")
    let invalid_try_float = "invalid".try_to_float()
    println("调用成功，返回值已创建")

    // 测试try_to_float()方法（有效输入应该返回Some）
    println("\n测试try_to_float()方法（有效输入）")
    println("期望: 返回Some(123.45)")
    let valid_try_float = "123.45".try_to_float()
    println("调用成功，返回值已创建")
}

// 主函数
func main() {
    test_to_float_vs_try_to_float()
}

// 调用主函数
main()