func assert(x) { __builtin_assert(x) }

let v1 = Option#some(2)
let v2 = v1.map(func(x: int) -> x * 2)
assert(match v2 { Option#some(y) { y } _ { 0 } } is 4)

let none = Option#none
let v4 = none.or(7)
assert(v4 is 7)
