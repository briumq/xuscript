let d: {string: int} = {};
d.insert("a", 1);
let v1 = d.get("a");
match v1 { Option#some(x) { print(x); } _ { print(0); } }
print(d.length());
d.insert_int(1, 100);
let v2 = d.get_int(1);
match v2 { Option#some(x) { print(x); } _ { print(0); } }
let other: {string: int} = {};
other.insert("b", 2);
d.merge(other);
print(d.length());
let ks = d.keys();
print(ks.length());
let vs = d.values();
print(vs.length());
print(d.contains("a"));
print(d.remove("a"));
print(d.length());
d.clear();
print(d.length());
