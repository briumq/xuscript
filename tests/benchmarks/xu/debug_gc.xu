
var BENCH_SCALE = "1000"
func now_us() -> int { return mono_micros() }
func run_min_ms(repeat: int, logic) -> float {
  logic()
  return 0.0
}
func out_json(case: string, N: int, ms: float) { print(case) }

func bench_string_scan(N: int) {
    var s = ""
    for i in [0..100] { s = s + "0123456789" }
    let b = builder_new()
    for c in s { builder_push(b, c) }
    let _s = builder_finalize(b)
}

bench_string_scan(1000)
print("Done")
