func build_index(docs: {int: string}) -> {string: [int]} {
    let idx = {}
    for id in docs.keys() {
        let terms = docs[id].split(" ")
        for t in terms {
            if idx.has(t) { idx[t].add(id) } else { idx[t] = [id] }
        }
    }
    return idx
}
