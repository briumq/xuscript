Svc with [ ok | fail(msg: string) ]

func run(seq: [string]) -> Svc {
    for name in seq {
        if name is "db" { continue }
        if name is "cache" { continue }
        if name is "api" { continue }
    }
    return Svc#ok
}
