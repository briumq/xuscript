# 未完成功能跟踪列表（拟新增 docs/未完成功能列表.md）

## 范围与依据
- 依据：源码与当前 docs《Xu 语言规范 v1.1》中的功能项
- 目标：在 docs 中维护“未完成功能”清单，用于迭代跟踪

## 文档结构
- 标题与更新时间
- 说明：来源（规范/代码）、标记规则
- 清单：每项含简述、规范出处、代码引用与期望验收标准

## 初始清单（待落文档）
- Set 集合与字面量 set{}（规范有、代码缺）
  - 规范出处：规范集合章节
  - 代码引用：types.rs / value.rs 不含 Set
  - 验收：类型系统、运行时值、解析与基本方法
- 元组类型与字面量 (a, b, ...)（规范有、代码缺）
  - 规范出处：类型章节
  - 代码引用：types.rs / value.rs 不含 Tuple
  - 验收：类型系统、运行时值、解析与解构/访问
- 变量遮蔽禁止（shadowing）（规范要求、代码未见实现/测试）
  - 规范出处：变量与作用域章节
  - 代码引用：未检出相关逻辑与测试
  - 验收：编译期/运行期检查、错误码与文档用例
- 枚举一般构造与变体访问的端到端用例覆盖（代码实现较全，但测试与示例需齐）
  - 规范出处：枚举章节
  - 代码引用：runtime 方法齐备，端到端示例不足
  - 验收：以规范风格的 E2E 测试与文档示例

## 维护约定
- 每次功能落地：更新文档并移动到“已完成”清单（含提交引用）
- 可选增强：在 CI 增加扫描脚本（标记未实现类型/字面量/语义约束）

## 输出位置
- 新建文件：docs/未完成功能列表.md

请确认以上计划，我将创建该文档并填入上述初始清单与引用链接。