# 未完成功能跟踪列表

本文档依据《Xu 语言规范 v1.1》与当前代码实现（v1.1-dev）的差异整理，用于迭代跟踪。

## 规范差距跟踪表

| 功能项 | 规范出处 | 当前支持度 | 证据（测试/代码） | 缺口说明 | 下一步 |
|---|---|---|---|---|---|
| **match 默认分支用 `_` 通配符（替代 `else`）** | 设计说明见规范与 [syntax_refactoring_plan.md](file:///Users/hencoo/projects/xuscript/docs/syntax_refactoring_plan.md) | 🟢 已支持 | parser 以最后一个 `_` 分支作为默认分支；相关用例见 [match_expression.xu](file:///Users/hencoo/projects/xuscript/tests/specs/match_expression.xu) | - | - |
| **`let (a, _) = (...)` 的 `_` 忽略语义** | 4.1 元组解构与通配符 | 🟢 已支持 | `_` 在解构赋值中不再绑定变量；用例见 [tuple_features_check.xu](file:///Users/hencoo/projects/xuscript/tests/specs/tuple_features_check.xu) | - | - |
| **结构体字段默认值 `age: int = 0`** | 5.3 结构体 `has` | 🟢 已支持 | 语法与 AST 支持 default；运行时 StructInit 会用默认值预填充；用例见 [struct_field_defaults.xu](file:///Users/hencoo/projects/xuscript/tests/specs/struct_field_defaults.xu) | - | - |
| **Tuple 元组类型（静态类型检查）** | 5.2 集合类型、6.1 多返回值 | 🟡 部分支持 | 运行时支持元组值与访问（见 [value.rs](file:///Users/hencoo/projects/xuscript/crates/xu_runtime/src/value.rs)），但类型系统把 tuple 归为 any | 规范允许 `-> (int, int)` 这种返回类型并应参与检查；当前不会做细粒度校验 | 扩展 TypeInterner/typeref 以支持 tuple[T1,T2...]，补类型推断与 mismatch 用例 |
| **Set 集合类型（静态类型检查）** | 5.2 集合类型 | 🟡 部分支持 | 运行时支持 set 与方法（见 [set_methods.xu](file:///Users/hencoo/projects/xuscript/tests/specs/set_methods.xu)），但类型系统把 set 归为 any | `{int}` 这类集合类型标注目前不参与检查 | 扩展类型系统支持 set[T] 并补 tests |
| **“空容器必须显式标注”的负向用例覆盖** | 5.2/5.7 | 🟢 已支持 | 诊断实现见 [analyzer.rs](file:///Users/hencoo/projects/xuscript/crates/xu_driver/src/analyzer.rs)，负向用例见 [empty_list_requires_type.invalid.xu](file:///Users/hencoo/projects/xuscript/tests/specs/empty_list_requires_type.invalid.xu)、[empty_dict_requires_type.invalid.xu](file:///Users/hencoo/projects/xuscript/tests/specs/empty_dict_requires_type.invalid.xu)、[empty_set_requires_type.invalid.xu](file:///Users/hencoo/projects/xuscript/tests/specs/empty_set_requires_type.invalid.xu) | - | - |

## 待优化项

| 项目 | 说明 |
|---|---|
| **JSON 模块** | 标准库 `json` 模块仅支持基础类型，不支持嵌套结构体的自动序列化 |
| **File 模块** | 文件操作 API 尚未统一整理进 `stdlib/file.xu` |
| **错误信息** | 部分运行时错误信息需要对齐《Xu 语言错误信息规范》 |

## 已完成 (示例)

| 功能项 | 完成时间 | Commit |
|---|---|---|
| (暂无) | - | - |
